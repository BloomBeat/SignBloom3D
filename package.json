{
  "name": "signbloom3d-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "pnpm --filter client run dev",
    "server": "pnpm --filter server run dev",
    "dev": "pnpm run server & pnpm run client",
    "build": "pnpm --filter client run build",
    "start": "pnpm --filter server run start"
  }
}